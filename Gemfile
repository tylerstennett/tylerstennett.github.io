source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick" # required for `jekyll serve` on Ruby >= 3.0

# Stdlib gems no longer bundled with Ruby >= 3.4 that Jekyll still requires.
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"
